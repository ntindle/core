syntax = "proto3";

package common.crypto;
option go_package = "github.com/sonr-io/core/common/crypto";

// TokenType is the type of keychain.
enum TokenType {
    TOKEN_TYPE_UNSPECIFIED = 0;
    TOKEN_TYPE_SNR = 1;
    TOKEN_TYPE_ETH = 2;
    TOKEN_TYPE_BTC = 3;
    TOKEN_TYPE_SOL = 4;
}
